# i18next Многоязычность - Инструкция

Это приложение использует **i18next** для поддержки трёх языков: **Русский**, **Английский** и **Кыргызский**.

## Как это работает

### 1. Установленные пакеты

```bash
npm install i18next react-i18next i18next-browser-languagedetector
```

### 2. Структура файлов локализации

Файлы переводов находятся в папке `src/locales/`:

- `en.json` - английский язык
- `ru.json` - русский язык
- `ky.json` - кыргызский язык

### 3. Как использовать переводы в компонентах

#### Базовое использование useTranslation hook:

```jsx
import { useTranslation } from "react-i18next";

const MyComponent = () => {
  const { t } = useTranslation();

  return (
    <div>
      <h1>{t("home.welcome")}</h1>
      <p>{t("header.home")}</p>
    </div>
  );
};
```

#### Переключение языка:

Используйте компонент `LanguageSwitcher`, который уже встроен в Header:

```jsx
import LanguageSwitcher from "./components/LanguageSwitcher";

<LanguageSwitcher />;
```

### 4. Добавление новых переводов

1. Откройте файл `src/locales/en.json`
2. Добавьте новый ключ перевода:

```json
{
  "mySection": {
    "myKey": "My translation"
  }
}
```

3. Добавьте тот же ключ в `ru.json` и `ky.json`
4. Используйте в компонентах:

```jsx
{
  t("mySection.myKey");
}
```

### 5. Ключи переводов в коде

#### Header & Navigation

- `header.home` - Главная/Home
- `header.about` - О клинике/About
- `header.services` - Услуги/Services
- `header.store` - Магазин/Store
- `header.register` - Регистрация/Register
- `header.login` - Вход/Login
- `header.profile` - Профиль/Profile
- `header.cta` - Записаться/Book

#### Home Page

- `home.welcome` - Добро пожаловать
- `home.mainTitle` - Главный заголовок
- `home.mainSubtitle` - Подзаголовок
- `home.ourServices` - Наши услуги
- `home.ourDoctors` - Наши врачи
- `home.advantages` - Наши преимущества

#### Services

- `services.title` - Наши услуги
- `services.cleaning` - Чистка
- `services.whitening` - Отбеливание
- `services.implants` - Имплантация
- `services.price` - Цена
- `services.duration` - Длительность

#### Store

- `store.title` - Магазин
- `store.price` - Цена
- `store.empty` - Пусто
- `store.add` - Добавить в корзину
- `store.addToWishlist` - Добавить в список

#### Common UI

- `common.yes` - Да/Yes
- `common.no` - Нет/No
- `common.save` - Сохранить/Save
- `common.cancel` - Отмена/Cancel
- `common.delete` - Удалить/Delete
- `common.search` - Поиск/Search
- `common.noResults` - Результаты не найдены

### 6. Тестирование переводов

После изменения переводов:

1. Сохраните файл JSON
2. Приложение автоматически перезагрузится
3. Используйте `LanguageSwitcher` для смены языка в Header

### 7. Обновление компонентов

Обновлённые компоненты с поддержкой i18next:

- ✅ Header.jsx
- ✅ Footer.jsx
- ✅ Home/Main.jsx
- ✅ Services.jsx
- ✅ Store.jsx
- ✅ About/US.jsx
- ✅ LanguageSwitcher.jsx

### 8. Добавление поддержки i18next в новые компоненты

1. Импортируйте hook:

```jsx
import { useTranslation } from "react-i18next";
```

2. Используйте в компоненте:

```jsx
const { t } = useTranslation();
```

3. Замените все жестко закодированные тексты на `t('key')`:

```jsx
// Было:
<button>Отправить</button>

// Стало:
<button>{t('common.submit')}</button>
```

### 9. Язык по умолчанию

По умолчанию приложение будет использовать язык браузера (если он совпадает с поддерживаемыми).
Если язык браузера не поддерживается, будет использован английский.

Вы можете изменить это в `src/i18n.js`:

```javascript
fallbackLng: "en"; // Язык по умолчанию
```

### 10. Сохранение выбранного языка

Выбранный язык сохраняется в localStorage и автоматически загружается при открытии приложения.

## Поддерживаемые языки

| Код | Язык     | Flagi |
| --- | -------- | ----- |
| en  | English  | 🇬🇧    |
| ru  | Русский  | 🇷🇺    |
| ky  | Кыргызча | 🇰🇬    |

## Дополнительная информация

- Документация i18next: https://www.i18next.com/
- react-i18next docs: https://react.i18next.com/

## Вопросы?

Если у вас есть вопросы о настройке или использовании i18next, обратитесь к документации или проверьте примеры в обновленных компонентах.
